"use strict";
/**
 * aquiplex_billing_v2/.../models/Payment.js
 *
 * WARNING: This file is NOT imported by the active server (index.js loads
 * from ../../models/Payment). This copy is kept for reference only.
 * All production imports must use: require("../../../models/Payment")
 *
 * This file is intentionally identical to models/Payment.js to prevent
 * schema conflicts if it is ever accidentally imported.
 */

// Re-export the canonical model so any accidental import still works correctly.
module.exports = require("../../../models/Payment");
