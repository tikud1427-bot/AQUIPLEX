<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Aquiplex — AI Workflows Made Simple</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/landing.css">
</head>

<body class="landing-page">

  <!-- 🔝 NAVBAR -->
  <header class="navbar">
    <div class="logo">
      <img src="https://aquiplex.com/logo/logo.png" alt="Aquiplex Logo">
      <span>Aquiplex</span>
    </div>

    <div class="auth-buttons">
      <a href="/login" class="nav-btn secondary">Login</a>
      <a href="/signup" class="nav-btn primary">Sign Up</a>
    </div>
  </header>

  <!-- 🚀 HERO -->
  <section class="hero">

    <div class="ai-wave"></div>
    <div class="floating-shape shape1"></div>
    <div class="floating-shape shape2"></div>
    <div class="floating-shape shape3"></div>

    <!-- ✅ FIXED TEXT BREAK -->
    <h1>
      Build the Perfect  
      <span class="gradient-text">AI Workflow</span><br>
      for Anything
    </h1>

    <p>
      Tell us your goal — Aquiplex builds your perfect set of AI tools instantly.
    </p>

    <div class="hero-buttons">
      <a href="/app" class="primary-btn">
        🚀 Use Now — No Signup
      </a>

      <a href="#how-it-works" class="secondary-btn">
        ▶ See How It Works
      </a>
    </div>

    <!-- 🔥 FIXED INPUT -->
    <div class="demo-box">
      <input id="demoInput" type="text" placeholder="Create a YouTube video..." readonly>
      <button>Generate</button>
    </div>

    <!-- ✅ FIXED WRAP -->
    <div class="examples">
      <span>Write an essay</span>
      <span>Start a business</span>
      <span>Grow on Instagram</span>
    </div>

  </section>

  <!-- 💡 HOW IT WORKS -->
  <section id="how-it-works" class="how-it-works">
    <h2>How It Works</h2>

    <div class="steps">

      <div class="step">
        <div class="step-icon">🎯</div>
        <h3>Tell Your Goal</h3>
        <p>Describe what you want to achieve in simple words.</p>
      </div>

      <div class="step">
        <div class="step-icon">⚙️</div>
        <h3>Get AI Bundle</h3>
        <p>We generate the best tools for your exact task.</p>
      </div>

      <div class="step">
        <div class="step-icon">🚀</div>
        <h3>Start Creating</h3>
        <p>Use tools instantly and get results faster.</p>
      </div>

    </div>
  </section>

  <!-- ✨ WHY -->
  <section class="why">
    <h2>Why Aquiplex?</h2>

    <div class="features">

      <div class="feature-card">
        <h3>⚡ Save Hours</h3>
        <p>No more searching. We do the work for you.</p>
      </div>

      <div class="feature-card">
        <h3>🧠 Smart AI</h3>
        <p>Real workflows, not random tool lists.</p>
      </div>

      <div class="feature-card">
        <h3>🧰 Workspace</h3>
        <p>Save and reuse your workflows anytime.</p>
      </div>

    </div>
  </section>

  <!-- 🌍 USE CASES -->
  <section class="use-cases">
    <h2>Use Cases</h2>

    <p class="subtitle">
      Choose what you want to do, and Aquiplex builds your AI workflow instantly.
    </p>

    <div class="use-grid">
      <div>🎥 YouTube Content</div>
      <div>📚 Study & Essays</div>
      <div>💼 Online Income</div>
      <div>📱 Social Growth</div>
      <div>🧑‍💻 Coding Projects</div>
    </div>
  </section>

  <!-- 🚀 CTA -->
  <section class="final-cta">

    <!-- ✅ BETTER BREAK -->
    <h2>
      Stop Searching.<br>
      Start Creating.
    </h2>

    <p>Build your AI workflow in seconds.</p>

    <a href="/app" class="primary-btn big-btn">
      🚀 Use Now
    </a>

  </section>

  <!-- 🧭 FOOTER -->
  <footer class="footer">
    <p>© 2026 Aquiplex</p>
  </footer>

  <!-- ✨ JS -->
  <script>
    const texts = [
      "Create a YouTube video...",
      "Write an essay...",
      "Start an online business...",
      "Grow on Instagram..."
    ];

    let i = 0;
    const input = document.getElementById("demoInput");

    setInterval(() => {
      input.placeholder = texts[i];
      i = (i + 1) % texts.length;
    }, 2000);
  </script>
  <script>

  /* =========================
  🔥 CURSOR GLOW
  ========================= */
  const glow = document.createElement("div");
  glow.classList.add("cursor-glow");
  document.body.appendChild(glow);

  document.addEventListener("mousemove", (e) => {
    glow.style.left = e.clientX + "px";
    glow.style.top = e.clientY + "px";
  });

  /* =========================
  🔥 SCROLL REVEAL
  ========================= */
  const reveals = document.querySelectorAll(".hero, section");

  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add("active");
      }
    });
  }, { threshold: 0.15 });

  reveals.forEach(el => {
    el.classList.add("reveal");
    observer.observe(el);
  });

  /* =========================
  🔥 SMART TYPING INPUT (UPGRADE)
  ========================= */
  const texts = [
    "Create a YouTube video...",
    "Write an essay...",
    "Start an online business...",
    "Grow on Instagram..."
  ];

  let i = 0;
  let j = 0;
  let current = "";
  let isDeleting = false;

  const input = document.getElementById("demoInput");

  function typeEffect() {
    current = texts[i];

    if (isDeleting) {
      input.placeholder = current.substring(0, j--);
    } else {
      input.placeholder = current.substring(0, j++);
    }

    if (!isDeleting && j === current.length) {
      isDeleting = true;
      setTimeout(typeEffect, 1200);
      return;
    }

    if (isDeleting && j === 0) {
      isDeleting = false;
      i = (i + 1) % texts.length;
    }

    setTimeout(typeEffect, isDeleting ? 40 : 70);
  }

  typeEffect();

  </script>

</body>
</html>